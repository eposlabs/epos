{
  "name": "paralayer",
  "version": "1.14.0",
  "type": "module",
  "license": "MIT",
  "author": "imkost",
  "description": "",
  "keywords": [
    "vite",
    "vite-plugin"
  ],
  "scripts": {
    "dev": "rimraf dist && tsgo --watch",
    "build": "rimraf dist && tsgo && chmod +x dist/bin.js",
    "lint": "tsgo --noEmit",
    "release": "sh -c 'npm version ${1:-minor} && npm run build && npm publish' --"
  },
  "bin": {
    "paralayer": "./src/bin.js"
  },
  "exports": {
    ".": "./dist/paralayer.js"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "chokidar": "^5.0.0",
    "dropcap": "^1.5.0",
    "minimist": "^1.2.8"
  },
  "devDependencies": {
    "@types/minimist": "^1.2.5"
  }
}
