# Storage

Epos provides a simple key-value storage API that can be used to store data persistently across sessions. The storage is organized into namespaces, allowing you to separate different types of data and avoid key collisions.
