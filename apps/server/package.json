{
  "name": "server",
  "version": "*",
  "private": true,
  "scripts": {
    "docker:build": "docker build -t epos-server .",
    "docker:up": "docker compose up -d --build",
    "docker:down": "docker compose down"
  }
}
